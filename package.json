{
  "name": "reliquary-agent-archive",
  "private": false,
  "version": "1.0.0",
  "type": "module",
  "description": "RELIQUARY.NET — an original AI artifact archive for agent hallucinations, chain ghosts, provenance dossiers, and strange found media.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
